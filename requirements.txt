# SatelliteSense local development requirements.
#
# Core runtime dependencies (keep in sync with requirements-prod.txt).
Django==5.2.13
django-cors-headers==4.9.0
dashscope==1.25.16
numpy==2.2.5
Pillow==12.2.0
python-docx==1.2.0
python-dotenv==1.2.2
requests==2.33.1
jsonschema==4.26.0
tifffile==2026.3.3
lxml==6.1.0

# Optional: RemoteCLIP tile ranking (map_api/utils/clip_retriever.py).
# Without these the tile ranker falls back to a color/edge heuristic.
# GPU build: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# torch==2.12.0
# torchvision==0.27.0
# open_clip_torch==3.3.0
# timm==1.0.27

# Optional: packaging the --noreload launcher into a Windows exe.
# pyinstaller==6.19.0
# pyinstaller-hooks-contrib==2026.4

# Optional: extra test runners (manage.py test works without them).
# pytest==9.0.3
# pytest-django==4.12.0
# pytest-cov==7.1.0
